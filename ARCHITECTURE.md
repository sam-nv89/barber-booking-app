# Архитектура Проекта Antigravity (Barber Booking App)

Этот документ описывает высокоуровневую структуру, ключевые компоненты и потоки данных приложения.

## 📂 Структура Директорий

### `src/`
Корневая директория исходного кода.

*   **`components/`** - Reusable UI компоненты и функциональные блоки.
    *   `ui/` - Базовые UI элементы (Button, Input, Card), стилизованные через Tailwind.
    *   `layout/` - Компоненты разметки (Layout, BottomNav).
    *   `features/` - Сложные бизнес-компоненты (MasterBookingModal, Chat, DateTimeSelector).
    *   `providers/` - React Context провайдеры (TMAProvider для Telegram интеграции).
*   **`pages/`** - Компоненты страниц (Route handlers).
    *   `master/` - Страницы для мастеров (Dashboard, Records, Settings).
    *   `client/` - Страницы для клиентов (Booking, Profile).
*   **`store/`** - Управление состоянием.
    *   `useStore.js` - Глобальный Zustand стор. Содержит логику заявок, пользователей, настроек салона.
*   **`hooks/`** - Кастомные React хуки.
    *   `useTelegram.js` - Хук для взаимодействия с Telegram WebApp API.
    *   `useTelegramTheme.js` - Логика синхронизации темы с Telegram.
    *   `useHaptic.js` - Управление вибрацией (Haptic Feedback).
*   **`lib/`** - Утилиты и константы.
    *   `utils.js` - Общие вспомогательные функции (cn, formatPrice).

## 🏗 Ключевые Архитектурные Решения

### 1. Telegram Mini App (TMA) First
Приложение спроектировано как "нативное" для Telegram.
*   **TMAProvider**: Обертывает приложение, инициализирует SDK, управляет темой и безопасными зонами.
*   **Навигация**: Использует `react-router-dom`, но интегрируется с нативной кнопкой "Назад" Telegram (`BackButton`).
*   **Стилизация**: Использует CSS Variables (`--background`, `--text-color`), которые динамически маппятся из параметров темы Telegram (`themeParams`).

### 2. State Management (Zustand)
Используется **Zustand** для глобального состояния.
*   **Монолитный стор (`useStore.js`)**: Содержит слайсы для:
    *   `appointments` (Записи)
    *   `masters` (Мастера)
    *   `clients` (Клиенты)
    *   `salonSettings` (Настройки салона)
    *   `user` (Текущий пользователь)
*   Данные персистируются в `localStorage` (через middleware `persist`).

### 3. Ролевая Модель
Приложение разделяет логику для двух типов пользователей:
*   **Мастер/Владелец**: Управление записями, настройка графика, статистика.
*   **Клиент**: Онлайн-запись, просмотр истории, профиль.
(Логика разделения реализована на уровне роутинга и проверок в компонентах).

### 4. UI/UX
*   **Tailwind CSS**: Основной инструмент стилизации.
*   **Адаптивность**: Mobile-first дизайн, оптимизированный под Webview Telegram.
*   **Темы**: Поддержка темной и светлой темы, полностью синхронизированная с настройками Telegram клиента.

## 🔄 Потоки Данных
1.  **Инициализация**: `TMAProvider` считывает `initData` из Telegram -> устанавливает пользователя в `useStore`.
2.  **Запись**: Клиент выбирает дату/время (`DateTimeSelector`) -> Вызывает экшен `addAppointment` в сторе -> Обновление UI у мастера (Optimistic UI).

## 🛠 Технический Стек
*   React + Vite
*   Tailwind CSS
*   Zustand
*   React Router DOM
*   @telegram-apps/sdk-react
*   date-fns
