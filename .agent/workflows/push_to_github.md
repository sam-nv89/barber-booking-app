---
description: Automatically commits and pushes all changes to GitHub
---

1. Check git status
// turbo
2. Add all changes
```bash
git add .
```

3. Commit changes (with a generic update message if none provided)
```bash
git commit -m "Update: Auto-push from Antigravity"
```

4. Push to origin
```bash
git push
```
